name: CI

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]
  workflow_dispatch:

jobs:
  build-and-test:
    name: Build & test (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]

    steps:
      - uses: actions/checkout@v4

      - name: Setup .NET 10
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.0.x'

      - name: Restore
        run: dotnet restore PaddleOcrNet.sln

      - name: Build (Release)
        run: dotnet build PaddleOcrNet.sln -c Release --no-restore

      - name: Test (unit only — integration tests need model downloads)
        run: dotnet test test/PaddleOcrNet.Tests/PaddleOcrNet.Tests.csproj -c Release --no-build --filter "Category!=Integration" --logger "trx;LogFileName=test-results.trx"

      - name: Upload test results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: test-results-${{ matrix.os }}
          path: '**/test-results.trx'
          if-no-files-found: ignore

  aot-smoke:
    name: Native AOT publish smoke (informational)
    runs-on: ubuntu-latest
    needs: build-and-test
    # Surfaces trim/AOT incompatibilities (the library advertises IsAotCompatible) without blocking PRs.
    continue-on-error: true
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.0.x'
      - name: Install Native AOT prerequisites
        run: sudo apt-get update && sudo apt-get install -y clang zlib1g-dev
      - name: Publish demo with Native AOT
        run: dotnet publish test/PaddleOcrNet.Demo/PaddleOcrNet.Demo.csproj -c Release -r linux-x64 -p:PublishAot=true -o ./aot-out

  pack:
    name: Pack (validate nupkg builds)
    runs-on: ubuntu-latest
    needs: build-and-test
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.0.x'
      - name: Pack library
        run: dotnet pack src/PaddleOcrNet/PaddleOcrNet.csproj -c Release -o ./artifacts
      - name: Upload package artifact
        uses: actions/upload-artifact@v4
        with:
          name: nupkg
          path: ./artifacts/*.nupkg
